> ⚠️ DEPRECATED — NON-AUTHORITATIVE
>
> This document is NOT canonical.
> It SHALL NEVER be used as a source of governance.
> Canonical governance will reside exclusively in `directive_core/docs/`.
> This file exists only for historical or migration reference.

{
  "$id": "https://schemas.sswg.local/pdl-phases/compare.json#",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "sswg pdl phase: compare",
  "$defs": {
    "phase": {
      "allOf": [
        { "$ref": "_common.json#/$defs/phase_base" },
        {
          "type": "object",
          "properties": {
            "name": { "const": "compare" },
            "type": { "const": "comparison" },
            "constraints": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "deterministic_required": { "type": "boolean", "const": true },
                "overlap_metrics_allowed": {
                  "type": "array",
                  "items": { "type": "string", "enum": ["iou", "jaccard"] },
                  "minItems": 1
                }
              },
              "required": ["deterministic_required", "overlap_metrics_allowed"]
            }
          },
          "required": ["constraints"]
        }
      ]
    }
  },
  "$ref": "#/$defs/phase"
}
